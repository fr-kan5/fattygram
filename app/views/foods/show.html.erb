<section class="hero is-small is-info">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        フード詳細
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-7">
        <div class="card">
          <div class="card-image">
            <figure class="image is-4by3">
              <%= attachment_image_tag @food, :image %>
            </figure>
          </div>
          <div class="card-content">
            <div class="media">
              <div class="media-content">
                <p class="title is-4"><%= @food.title %></p>
              </div>
            </div>
            <div class="content">
              <table class="table is-narrow">
                <tr>
                  <th>概要</th>
                </tr>
                <tr>
                  <td><%= simple_format @food.body %></td>
                </tr>
              </table>
              <% if @food.user.id == current_user.id %>
                <%= link_to "編集画面へ", edit_food_path(@food), class: "button is-success" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <div class="column is-4">
        <article class="panel is-link">
          <p class="panel-heading">
            <%= @food.user.username %>
          </p>
          <div class="panel-block">
            <p class="control">
              <%= @food.user.profile %>
            </p>
          </div>
          <%= link_to user_path(@food.user), class: "panel-block" do %>
            <span class="panel-icon">
              <i class="fas fa-user" aria-hidden="true"></i>
            </span>
          <%= @food.user.username %> さんのページへ
          <% end %>
        </article>
      </div>
    </div>
  </div>
</section>